spring:
  cloud:
    config:
      enabled: true
      fail-fast: true
      retry:
        initial-interval: 1000
        max-interval: 2000
        max-attempts: 3
      discovery:
        enabled: true
        service-id: config-server
  application:
    name: user-center
  profiles:
    active: dev 